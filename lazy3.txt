#include "lazy_util.txt"
#include "lazy_string.txt"
//#define "print" "boop yourself"
#include "lazy_print.txt"
//#undefine "print"
//and by "boop yourself" i mean "print"
//#define BEEP
#define "Hello World" "no\nu"
/*
	todo:
	fix stores/gets so very large expressions work
*/

float unreachable() while(1) end end

//float t1;
//float t2 = 500;
//float arr[6];

class vec2
	float X;
	float Y;
	float avg()
		return (X+Y)/2;
	end
	float switch()
		float Z = X;
		X = Y;
		Y = Z;
	end
end

float main()
	println("Hello World");
	class vec2 A;
	class vec2 B;
	B.X = 1;
	B.Y = 3;
	A.X = 5;
	A.Y = 7;
	print("A.avg() = ");printnum(A.avg());println("");
	print("B.avg() = ");printnum(B.avg());println("");
	print("A.X = ");printnum(A.X);print(", A.Y = ");printnum(A.Y);println("");
	A.switch();
	println("switched");
	print("A.X = ");printnum(A.X);print(", A.Y = ");printnum(A.Y);println("");
	//print("7*6 = ");printnum(7*6);println("");
	//println("yup");
	//print("strlen \"\" = ");printnum(strlen(""));println(" yep");
	while(1) end
end

/*
    float i = 0;
	float derp1 = 1000;
	//float arr[6];
	float derp10 = 10000;
	i = derp1 - 5;
	float *str = "Hello World";
	//strtest(str);
	float *str3 = "T = ";
	float str2[20];
	//str2[0] = 50;
	//str2[1] = 0;
	scr(0,-1);
	scr(0,strlen(str));
	scr(2,str2);
	itoa(str2,123);
	scr(1,strlen(str2));
	//float T = 0;
	printXY(str,0,0);
	printXY(str2,0,1);
	while(1)
		printXY("T = ",0,2);
		itoa(str2,T);
		//float l = strlen(str3);
		printXY(str2,strlen("T = "),2);
		T++;
	end
	
	while(1) end
end
*/

